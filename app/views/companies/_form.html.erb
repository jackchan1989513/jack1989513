<%= form_for(company) do |f| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Name %>
    <%= f.text_field :Name %>
  </div>

  <div class="field">
    <%= f.label :Authorization %>
    <%= f.text_area :Authorization %>
  </div>

  <div class="field">
    <%= f.label :Second %>
    <%= f.text_area :Second %>
  </div>

  <div class="field">
    <%= f.label :Other %>
    <%= f.text_area :Other %>
  </div>

  <div class="field">
    <%= f.label :Phone %>
    <%= f.text_field :Phone %>
  </div>

  <div class="field">
    <%= f.label :Email %>
    <%= f.text_field :Email %>
  </div>

  <div class="field">
    <%= f.label :Rack %>
    <%= f.text_field :Rack %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
