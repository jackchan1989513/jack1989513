<p id="notice"><%= notice %></p>

<h1>Can Not Move Outs</h1>

<table>
  <thead>
    <tr>
      <th>Companyname</th>
      <th>Equipment</th>
      <th>Reason</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @can_not_move_outs.each do |can_not_move_out| %>
      <tr>
        <td><%= can_not_move_out.CompanyName %></td>
        <td><%= can_not_move_out.Equipment %></td>
        <td><%= can_not_move_out.Reason %></td>
        <td><%= link_to 'Show', can_not_move_out %></td>
        <td><%= link_to 'Edit', edit_can_not_move_out_path(can_not_move_out) %></td>
        <td><%= link_to 'Destroy', can_not_move_out, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Can Not Move Out', new_can_not_move_out_path %>
