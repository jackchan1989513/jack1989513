<%= form_for(vendor_list) do |f| %>
  <% if vendor_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vendor_list.errors.count, "error") %> prohibited this vendor_list from being saved:</h2>

      <ul>
      <% vendor_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Name %>
    <%= f.text_field :Name %>
  </div>

  <div class="field">
    <%= f.label :Tel %>
    <%= f.text_field :Tel %>
  </div>

  <div class="field">
    <%= f.label :WorkTime %>
    <%= f.text_field :WorkTime %>
  </div>

  <div class="field">
    <%= f.label :Type %>
    <%= f.text_field :Type %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
